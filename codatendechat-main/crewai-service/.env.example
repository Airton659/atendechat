# Google Cloud
GOOGLE_CLOUD_PROJECT=seu-projeto-gcp
GOOGLE_CLOUD_LOCATION=us-central1
GOOGLE_APPLICATION_CREDENTIALS=/opt/crewai/google-credentials.json

# Vertex AI
# Modelos disponíveis (2025):
# - gemini-2.5-flash (RECOMENDADO - Melhor custo-benefício, GA, quota alta)
# - gemini-2.5-flash-lite (Mais rápido e barato, GA, quota muito alta)
# - gemini-2.0-flash (Multimodal avançado, GA, quota média)
# - gemini-2.0-flash-lite (Baixa latência, GA, quota baixa)
# NOTA: Gemini 1.5 foi DESCONTINUADO em abril/2025
VERTEX_MODEL=gemini-2.5-flash

# Rate Limiting e Concorrência
# Número máximo de requisições simultâneas ao LLM (Gemini)
# Recomendado: 10 para balanceamento, 5 para mais seguro, 15 para mais agressivo
MAX_CONCURRENT_LLM_REQUESTS=10

# Validação Programática
# Sistema híbrido: Validações (100% precisão) + Sugestões (90-95% precisão via IA)
# Ativa/desativa sistema de validação automática
ENABLE_VALIDATION_HOOKS=true
# Nível de rigor padrão para novas regras: low | medium | high
DEFAULT_VALIDATION_STRICTNESS=medium
# Timeout para execução de validações (ms)
VALIDATION_TIMEOUT=2000

# Servidor
HOST=0.0.0.0
PORT=8000
NODE_ENV=production

# CORS
CORS_ORIGINS=https://www.atendeaibr.com,http://localhost:3001

# Backend Node.js (Docker)
# URL do backend para ferramentas de agendamento, arquivos, etc
BACKEND_URL=http://localhost:3000
