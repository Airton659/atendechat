FROM node:20-alpine

WORKDIR /app

# Install serve globally
RUN npm install -g serve

# Copy ONLY the pre-built files
COPY frontend/build ./build

EXPOSE 3001

ENV HOST=0.0.0.0
ENV PORT=3001

# Serve the built application
CMD ["serve", "-s", "build", "-l", "3001"]
